NODE_ENV=development
PORT=
#app HOSTNAME
HOST_NAME=

DB_HOST=
DB_PORT=
DB_USER=
DB_PASSWORD=
DB_NAME=film_database

#JWT
JWT_SECRET=super_secret_change_meme
JWT_EXPIRES_IN=7d

# Email / SMTP for password reset
# Gmail dev example:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_16_character_app_password
# SMTP_FROM="Film Streaming <your_email@gmail.com>"
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# Subscription/VIP cron
# Default trong code là 1 giờ nếu không set. Ví dụ 10 phút = 600000
SUBSCRIPTION_CRON_INTERVAL_MS=3600000
# Đặt true nếu muốn tắt cron reconcile subscription
DISABLE_SUBSCRIPTION_CRON=false

# Local media pipeline / HLS
# Mặc định nếu bỏ trống MEDIA_STORAGE_ROOT thì dùng <project>/uploads/media
MEDIA_STORAGE_ROOT=
# Giới hạn upload video nguồn (byte). Mặc định trong code là 10GB
MEDIA_UPLOAD_MAX_BYTES=10737418240
# Polling interval cho worker xử lý media job
MEDIA_JOB_INTERVAL_MS=5000
# Đặt true nếu muốn tắt worker xử lý video nền
DISABLE_MEDIA_PROCESSING_CRON=false
# Giới hạn quality output tối đa. Ví dụ 720 để chỉ encode tới 720p
MEDIA_MAX_OUTPUT_HEIGHT=1080
# FFmpeg preset/crf và HLS segment config
MEDIA_FFMPEG_PRESET=veryfast
MEDIA_FFMPEG_CRF=21
MEDIA_HLS_SEGMENT_SECONDS=6
MEDIA_HLS_GOP=48

# Recommendation ML cron
# Mặc định chạy Python ML mỗi 3 phút = 180000ms
RECOMMENDATION_CRON_INTERVAL_MS=180000
# Đặt true nếu muốn tắt cron gợi ý phim
DISABLE_RECOMMENDATION_CRON=false
# Bỏ trống để tự dùng Python_Recommend-Film/.venv-ml nếu có, fallback sang python trong PATH
RECOMMENDATION_PYTHON_COMMAND=
RECOMMENDATION_ML_DIR=
RECOMMENDATION_ML_SCRIPT=ml_train_hybrid_als.py

# VNPay
VNP_TMNCODE=
VNP_HASH_SECRET=
VNP_URL=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
VNP_API_URL=https://sandbox.vnpayment.vn/merchant_webapi/api/transaction
VNP_API_IP=127.0.0.1
# Public backend callback URLs, ví dụ qua ngrok:
# https://<your-backend-tunnel>/api/payments/vnpay/return
# https://<your-backend-tunnel>/api/payments/vnpay/ipn
VNP_RETURN_URL=
VNP_IPN_URL=
VNP_TIMEZONE=Asia/Ho_Chi_Minh
VNP_LOCALE=vn
VNP_ORDER_TYPE=other
VNP_EXPIRE_MINUTES=15

# Optional: sau khi backend nhận return từ VNPay sẽ redirect tiếp về frontend URL này.
# Khi dev local có thể dùng: http://localhost:3000/payment-result
PAYMENT_RETURN_REDIRECT_URL=http://localhost:5173/
